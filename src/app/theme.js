import { createTheme } from '@mui/material';

const theme = createTheme({
  palette: {
    type: 'dark',
    primary: {
      main: '#f0f0f0',
    },
    secondary: {
      main: '#4caf50',
    },
  },
});

export default theme;
